---
title: Quick Start
description: Quick start for app developers using the MRS Flutter SDK.
---

## Prerequisites

- Flutter SDK installed
- Dart SDK compatible with this package's `pubspec.yaml`

## Install

Use the latest version listed on [pub.dev package page](https://pub.dev/packages/mrs_sdk_flutter), then add it to your app `pubspec.yaml`:

```yaml
dependencies:
  mrs_sdk_flutter: ^0.3.0
```

Then fetch dependencies:

```bash
flutter pub get
```

## Minimal Setup

```dart
import 'package:mrs_sdk_flutter/mrs_sdk_flutter.dart';

final spokeZone = SpokeZone(
  config: SpokeZoneConfig.device(
    deviceAuth: DeviceAuthCallbacks(
      cpuId: () async => 'cpu-id',
      uuid: () async => 'uuid',
      deviceId: () async => 'device-id',
      initialDeviceToken: () async => 'device-token',
    ),
  ),
);
```

## Example Usage

```dart
final device = await spokeZone.devices.get(123);
final dataFileId = await spokeZone.dataFiles.create('log');
await spokeZone.dataFiles.upload(dataFileId, payloadBytes);
```

## Reference

- [pub.dev Package](https://pub.dev/packages/mrs_sdk_flutter)
- [Package Documentation](https://pub.dev/documentation/mrs_sdk_flutter/latest/)
