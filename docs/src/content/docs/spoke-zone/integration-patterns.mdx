---
title: Integration Patterns
description: Minimal guidance for choosing auth mode and handling reliability.
sidebar:
  order: 2
---

## Choose Auth Mode

| Use case                            | Auth mode                     |
| ----------------------------------- | ----------------------------- |
| Embedded device telemetry/workflows | `SpokeZoneConfig.device(...)` |
| User-account driven app workflows   | `SpokeZoneConfig.user(...)`   |

## Reliability Expectations

- SDK retries transport errors, `429`, and `5xx` automatically.
- SDK does not retry non-`429` `4xx` responses.
- Failures are surfaced as `SpokeZoneException` with typed error codes.
