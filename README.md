# MRS SDK - Flutter

`mrs_sdk_flutter` is the official Flutter package for integrating mobile apps with MRS Electronic products and services.

## Installation

Add the package to your `pubspec.yaml`:

```yaml
dependencies:
  mrs_sdk_flutter: ^0.4.2
```

Then fetch dependencies:

```bash
flutter pub get
```

## Usage

Import the package in your Dart code:

```dart
import 'package:mrs_sdk_flutter/mrs_sdk_flutter.dart';
```

## Documentation

Full documentation lives at https://flutter.mrs-electronics.dev.

## Support

Report issues at https://github.com/mrs-electronics-inc/mrs-sdk-flutter/issues.
